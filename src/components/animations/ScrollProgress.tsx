import React from 'react';

interface ScrollProgressProps {
  className?: string;
  color?: string;
}

const ScrollProgress: React.FC<ScrollProgressProps> = () => {
  return null; // Barra de progresso desativada
};

export default ScrollProgress;

